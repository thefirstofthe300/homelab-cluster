apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: authentik
resources:
  - helm.yaml
  - namespace.yaml
  - postgres.yaml
  - sealedsecret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: sws-config
    files:
      - configs/config.toml
  - name: webfinger
    files:
      - configs/webfinger
